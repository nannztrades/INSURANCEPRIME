
# -------------------------------------------------------------------
# INSURANCEPRIME / FastAPI — Environment template (no real secrets)
# Copy this to `.env` for local dev. In production (Railway),
# set each key as a Project Variable (no .env file committed).
# -------------------------------------------------------------------

# ---- Database (MySQL) ----
# Local dev example. In Railway, use the service host and port (often 3306).
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_local_user
DB_PASSWORD=your_local_password
DB_NAME=insurancelocal

# Optional consolidated URL (only if your code supports it)
# DATABASE_URL=mysql+pymysql://your_local_user:your_local_password@localhost:3306/insurancelocal

# ---- Upload defaults (optional fallbacks used by uploads endpoints) ----
# Leave empty unless you want a default applied when a client omits fields.
DEFAULT_AGENT_CODE=
DEFAULT_AGENT_NAME=

# ---- Google Cloud Storage (if you store PDFs or exports) ----
# GCS_CREDENTIALS_BASE64 should be a base64-encoded Service Account JSON.
# Never commit the raw JSON file. Use base64 to store it as an env var safely.
GCS_BUCKET_NAME=your-bucket-name
GCS_CREDENTIALS_BASE64=base64-encoded-service-account-json
GCS_LOCATION=us-central1
GCS_PROJECT_ID=your-gcp-project-id
GCS_STORAGE_CLASS=STANDARD

# ---- Auth & Cookies ----
# In production, set AUTH_COOKIE_SECURE=1 so browsers mark the cookie Secure.
AUTH_COOKIE_SECURE=0
AUTH_COOKIE_SAMESITE=lax
# How long an auth token should be valid (in minutes)
AUTH_EXP_MINUTES=1440

# ---- CORS ----
# Add your local and production frontends here, comma-separated.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8501
